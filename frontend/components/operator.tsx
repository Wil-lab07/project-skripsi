import { Flex } from "@chakra-ui/react"

export const Operator = (props: {addr: string | undefined}) => {
    return (
        <>
            <Flex 
                direction={'column'} 
                justifyContent={'center'} 
                alignItems={'center'}
                height={'100vh'}
            >
                
            </Flex>
        </>
    )
}